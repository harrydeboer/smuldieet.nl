twig:
    default_path: '%kernel.project_dir%/templates'
    number_format:
        decimals: 0
        decimal_point: ','
        thousands_separator: ''

when@test:
    twig:
        strict_variables: true
