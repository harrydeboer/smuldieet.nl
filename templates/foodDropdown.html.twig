<div class="dropdown">
    <input type="text" class="{{ foodType }}-name form-control dropdown-toggle" maxlength="255"
           value="{{ title }}"
           data-bs-toggle="dropdown" placeholder="{{ foodType == 'foodstuff' ? 'voedingsmiddel' : 'recept' }}"
           aria-expanded="false" {{ required }}>
    <div class="dropdown-menu dropdown-menu-food">
    </div>
</div>
