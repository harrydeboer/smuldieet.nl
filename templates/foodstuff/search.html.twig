{% for foodstuff in foodstuffs %}
    <div id="foodstuff_result_{{ foodstuff.id }}"
         class="foodstuff-result"
         data-piece-name="{{ foodstuff.pieceName }}"
         data-piece-weight="{{ foodstuff.pieceWeight }}">{{ foodstuff.name }}</div>
{% endfor %}
